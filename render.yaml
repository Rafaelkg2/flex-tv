services:
  - type: web
    name: flex-tv
    runtime: node
    buildCommand: npm install
    startCommand: node server/index.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: DASHBOARD_PASSWORD
        sync: false
